<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=Edge,chrome=1">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "imdb_better" %></title>
    <%= csrf_meta_tags %>
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
    <%= csrf_meta_tags %>

    <!-- Le HTML5 shim, for IE6-8 support of HTML elements -->
    <!--[if lt IE 9]>
      <script src="http://html5shim.googlecode.com/svn/trunk/html5.js" type="text/javascript"></script>
    <![endif]-->

    <%= stylesheet_link_tag "readable", :media => "all" %>

    <%= yield(:page_stylesheet) if content_for?(:page_stylesheet) %>

  </head>
  <body>
    <div class="navbar navbar-inverse navbar-fixed-top">
      <div class="container">
        <div class="navbar-header">
          <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
          <a><%= link_to 'Imdb Better', root_path, :class => 'navbar-brand' %></a>
        </div>
        <div class="navbar-collapse collapse">
          <ul class="nav navbar-nav">
            <li><%= link_to 'Create Post', new_user_posts_path(:user_id) %></li>
            <% unless user_signed_in? %>
              <li><%= link_to 'Sign Up', new_user_registration_path(@user) %></li>
              <li><%= link_to 'Sign In', new_user_session_path %></li>
            <% else %>
              <%= link_to 'destroy all data' %>
              <li><%= link_to 'Sign Out', destroy_user_session_path(@user), method: :delete %></li>
            <% end %>
        </div><!--/.nav-collapse -->
      </div>
    </div>
    <div class='row'>
      <div class='col-md-4'>
      </div>
      <div class='col-md-4'>
        <br>
        <%= yield %>
        <div class="alert alert-danger" role="alert"> <h4 class='alert'><%= alert %></h4> </div>
      </div>
      <div class='col-md-4'>
      </div>
    </div>
    <%= javascript_include_tag "readable" %>
    <%= yield(:page_javascript) if content_for?(:page_javascript) %>
    <h4><%= params.inspect %></h4>
  </body>
</html>
