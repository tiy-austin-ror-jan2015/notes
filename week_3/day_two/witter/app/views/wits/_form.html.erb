<%= form_for(@wit) do |f| %>
  <% if @wit.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@wit.errors.count, "error") %> prohibited this wit from being saved:</h2>

      <ul>
      <% @wit.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class='form-group'>
    <%= f.label :message, "Message" %>
    <%= f.text_field :message, class: 'form-control', placeholder: 'My hilarious wit' %>
    <p class='help-block'>Type Your Message</p>
  </div>

  <div class="actions">
    <%= f.submit 'Save', class: 'btn btn-default' %>
  </div>
<% end %>